require("toggleterm").setup({
  open_mapping = [[<c-_>]],
  direction = "float",
  float_opts = {
    border = "curved",
  },
})

-- Lazygit is handled by snacks.nvim (Snacks.lazygit) in plugins/snacks.lua.
